# yaml-language-server: $schema=https://api-dev.gogitcms.com/schemas/go-git-cms.json
# GoGitCMS schema configuration
# See: https://docs.gogitcms.com/schema

version: 1
contentModels:
  - name: pages
    path: "src/content/pages/*.mdx"
    format: markdown
    markdown_flavor: mdx
    mdx_sources:
      - path: src/components/**/*.tsx
        exports: named
    previewUrl: "{{Filename}}"
    fields:
      - name: title
        type: string
        entry_title: true
  - name: articles
    path: "src/content/articles/*.mdx"
    previewUrl: /articles/{{Filename}}
    format: markdown
    markdown_flavor: mdx
    mdx_sources:
      - path: src/components/**/*.tsx
        exports: named
    fields:
      - name: title
        type: string
        entry_title: true
        required: true
      - name: description
        type: string
      - name: pubDate
        type: string
        display:
          component: date
      - name: category
        type: string
        enum:
          - all
          - design-management
          - engineering-management
          - web-development
          - systems-thinking
          - writing
        display:
          component: select
      - name: draft
        type: boolean