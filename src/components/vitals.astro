<script>
  import { webVitals } from '../scripts/web-vitals'

  const analyticsId = import.meta.env.PUBLIC_VERCEL_ANALYTICS_ID

  webVitals({
    path: window.location.pathname,
    analyticsId,
  })
</script>